<%= form_with(url: post_path, local: true, method: :delete) do |f| %>
  <%= hidden_field_tag :post_id, post.id %>
  <%= f.submit '削除', data: { confirm: "You sure?" },  class: 'btn btn-danger btn-sm' %>
<% end %>